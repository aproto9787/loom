{
  "name": "@loom/server",
  "version": "0.1.0",
  "license": "GPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "LOOM_SERVER_AUTOSTART=0 LOOM_MOCK=1 node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@loom/adapters": "workspace:^",
    "@loom/core": "workspace:^",
    "@loom/nodes": "workspace:^",
    "@loom/runtime": "workspace:^",
    "fastify": "^5.8.4",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  }
}
